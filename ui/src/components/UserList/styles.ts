import { makeStyles } from "@mui/styles";
export const useStyles = makeStyles({
  root: {
    width: "100%",
    maxWidth: 550,
    margin: "auto",
    padding: "100px",
  },
  listItem: {
    marginBottom: "8px", // Add margin bottom to create spacing between list items
  },
});
